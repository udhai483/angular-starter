import { Component } from '@angular/core';

@Component({
  selector: 'app-registration',
  standalone: true,
  template: `<h2>Registration Page Works!</h2>`, // We use 'template' instead of 'templateUrl' to bypass the file error for now
})
export class RegistrationComponent {}