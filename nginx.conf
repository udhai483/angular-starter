server {
    listen 8080;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Include mime types so CSS/JS load correctly
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
}